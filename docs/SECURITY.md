# 🔒 Документация по безопасности MeatBot

## Обзор мер безопасности

Данный документ описывает реализованные меры защиты от различных типов атак в Telegram-боте MeatBot.

---

## ✅ Защита от SQL-инъекций

### Реализовано:
- **SQLAlchemy ORM** - все запросы к БД выполняются через ORM
- **Параметризованные запросы** - ORM автоматически экранирует все параметры
- **Нет raw SQL** - за исключением health checks (`SELECT 1`)

### Пример безопасного кода:
```python
# ✅ БЕЗОПАСНО - параметризованный запрос через ORM
user = db.query(User).filter(User.id == user_id).first()

# ❌ НЕ ДЕЛАЙТЕ ТАК - raw SQL с пользовательскими данными
# db.execute(f"SELECT * FROM users WHERE id = {user_id}")
```

### Статус: 🟢 **ЗАЩИЩЕНО**

---

## ✅ Защита от XSS (Cross-Site Scripting)

### Реализовано:

#### 1. Валидация в пользовательских заказах:
- **Телефон** (`validate_phone_number`)
  - Удаление всех символов кроме цифр и `+`
  - Проверка формата через regex

- **Адрес** (`validate_address`)
  - Проверка длины (10-500 символов)
  - Блокировка подозрительных паттернов: `<script`, `javascript:`, `on\w+=`, `data:`

- **Комментарии** (`validate_delivery_notes`)
  - Ограничение длины (200 символов)
  - Блокировка подозрительных паттернов

#### 2. Валидация в админ-панели (ДОБАВЛЕНО):
- **Названия товаров** (`validate_product_name`)
  - Санитизация через `sanitize_text()`
  - Проверка длины (2-255 символов)
  - Блокировка: `<script`, `javascript:`, `on\w+=`, `data:`, `<iframe`, `<object`, `<embed`

- **Описания товаров** (`validate_product_description`)
  - Санитизация через `sanitize_text()`
  - Ограничение длины (1000 символов)
  - Блокировка опасных паттернов

- **Категории** (`validate_category_name`, `validate_category_description`)
  - Аналогичная валидация

### Функция санитизации:
```python
def sanitize_text(text: str) -> str:
    """Удаляет HTML теги и потенциально опасные символы"""
    # Удаляем HTML теги
    text = re.sub(r"<[^>]+>", "", text)
    # Удаляем опасные символы
    text = re.sub(r"[<>\"'&]", "", text)
    # Удаляем лишние пробелы
    text = re.sub(r"\s+", " ", text).strip()
    return text
```

### Статус: 🟢 **ЗАЩИЩЕНО**

---

## ✅ Защита от Code Injection

### Реализовано:
- **Нет eval/exec** - не используются динамическое выполнение кода
- **Типизация данных** - строгая типизация через SQLAlchemy models
- **Валидация всех входных данных**

### Статус: 🟢 **ЗАЩИЩЕНО**

---

## ✅ Защита от Path Traversal

### Реализовано для файлов изображений:
```python
class FileValidator:
    def __init__(self, allowed_extensions=[".jpg", ".jpeg", ".png", ".webp"]):
        self.allowed_extensions = allowed_extensions
        self.max_size = 5 * 1024 * 1024  # 5MB

    def validate(self, value):
        # Проверка расширения файла
        filename = value.filename.lower()
        if not any(filename.endswith(ext) for ext in self.allowed_extensions):
            raise ValidationError("File type not allowed")

        # Проверка размера
        if value.size > self.max_size:
            raise ValidationError("File size exceeds maximum")
```

### Статус: 🟢 **ЗАЩИЩЕНО**

---

## ✅ Реализованные меры защиты

### 1. Rate Limiting
**Статус:** ✅ **РЕАЛИЗОВАНО**

**Защита от:**
- DoS атак (Denial of Service)
- Spam
- Bruteforce

**Параметры:**
- Обычные пользователи: **30 запросов за 60 секунд**
- Администраторы: **100 запросов за 60 секунд**
- Автоматическая блокировка при превышении

**Реализация:**
```python
# meatbot/app/middlewares/rate_limit.py
rate_limiter = RateLimitMiddleware(
    rate_limit=30,
    time_window=60,
    admin_rate_limit=100,
)
```

**Логирование:**
- Все превышения лимита логируются
- Счетчик заблокированных запросов
- Топ пользователей по активности

### 2. Мониторинг безопасности
**Статус:** ✅ **РЕАЛИЗОВАНО**

**Отслеживает:**
- Попытки SQL injection
- Попытки XSS атак
- Подозрительные паттерны ввода
- Rate limiting нарушения
- Множественные ошибки валидации

**Автоматическая блокировка:**
- При 5+ критических событиях за час
- Блокировка на 24 часа
- Уведомление администраторов

**Использование:**
```python
from meatbot.app.services.security_monitor import security_monitor

# Логирование попытки injection
security_monitor.log_injection_attempt(
    user_id=user_id,
    input_text=text,
    detected_pattern="SQL injection",
    field_name="address",
)

# Просмотр статистики (команда /security_stats)
stats = security_monitor.get_statistics()
```

### 3. CSRF Protection
**Статус:** ✅ НЕ ТРЕБУЕТСЯ (Telegram Bot API использует токен-аутентификацию)

### 4. Input Sanitization
**Статус:** ✅ РЕАЛИЗОВАНО

- Функция `sanitize_text()` - удаление HTML и опасных символов
- Валидация всех пользовательских полей
- Логирование подозрительных попыток

### 5. Логирование безопасности
**Статус:** ✅ РАСШИРЕНО

Структурированное логирование через structlog:
```python
# Валидация
logger.warning("validation_failed", user_id=user_id, field="address")

# Rate limiting
logger.warning("rate_limit_exceeded", user_id=user_id, count=35)

# Security events
logger.error("injection_attempt_detected", user_id=user_id, pattern="SQL")
```

---

## 🧪 Тестирование безопасности

### Проведенные тесты:

#### SQL Injection:
```
Ввод: ;select * from orders
Результат: ✅ Заблокировано валидацией адреса
```

#### XSS:
```
Ввод: <script>alert('XSS')</script>
Результат: ✅ Удалено функцией sanitize_text()
```

#### Path Traversal:
```
Ввод: ../../etc/passwd
Результат: ✅ Недоступно (файлы загружаются только через Telegram API)
```

---

## 📋 Чеклист безопасности

- [x] SQL Injection - защита через ORM
- [x] XSS - валидация и санитизация
- [x] Code Injection - отсутствие eval/exec
- [x] Path Traversal - валидация файлов
- [x] Input Validation - все текстовые поля + мониторинг
- [x] File Upload Security - проверка типа и размера
- [x] Admin Access Control - middleware проверка прав
- [x] **Rate Limiting - РЕАЛИЗОВАНО** (30/мин для users, 100/мин для админов)
- [x] **Security Monitoring - РЕАЛИЗОВАНО** (автоматическая блокировка при атаках)
- [x] Logging - structlog для аудита + security events
- [x] Error Handling - обработка исключений без раскрытия деталей
- [x] **DoS Protection - РЕАЛИЗОВАНО** через Rate Limiting
- [x] **Attack Detection - РЕАЛИЗОВАНО** (SQL injection, XSS, etc.)

---

## 🔄 Обновления безопасности

### Версия 3.0 (текущая - 2025-10-25):
- ✅ **Rate Limiting** - защита от DoS и spam
- ✅ **Security Monitoring** - отслеживание атак и автоблокировка
- ✅ **Расширенная валидация** - детекция SQL injection, XSS
- ✅ **Мониторинг событий** - статистика безопасности
- ✅ **Документация** - руководства по обновлению зависимостей

**Новые файлы:**
- `meatbot/app/middlewares/rate_limit.py`
- `meatbot/app/services/security_monitor.py`
- `docs/DEPENDENCY_UPDATES.md`

### Версия 2.0:
- ✅ Добавлена валидация в админ-панели
- ✅ Создана утилита `admin_validation.py`
- ✅ Обновлены обработчики товаров и категорий

### Версия 1.0:
- ✅ Базовая валидация пользовательских заказов
- ✅ SQLAlchemy ORM для защиты от SQL-инъекций
- ✅ Функция `sanitize_text()`

---

## 📞 Контакты

При обнаружении уязвимостей безопасности, пожалуйста, свяжитесь с администратором проекта.

**Не публикуйте уязвимости публично!**

