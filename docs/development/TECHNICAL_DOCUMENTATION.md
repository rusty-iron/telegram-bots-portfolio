# 🔧 Техническая документация MeatBot

**Версия:** 1.0.0
**Дата обновления:** 20.10.2025
**Статус:** Актуально

---

## 🎯 Обзор

Данный документ содержит техническую документацию по разработке, включая настройку IDE, линтинг, FSM и другие технические аспекты.

---

## 📋 Содержание

1. [Настройка IDE](#настройка-ide)
2. [Линтинг и форматирование](#линтинг-и-форматирование)
3. [FSM (Finite State Machine)](#fsm-finite-state-machine)
4. [Архитектура системы](#архитектура-системы)
5. [Лучшие практики](#лучшие-практики)

---

## 💻 Настройка IDE

### Рекомендуемые IDE
- **VS Code** - с расширениями Python
- **PyCharm** - профессиональная IDE для Python
- **Vim/Neovim** - для опытных разработчиков
- **Sublime Text** - легковесный редактор

### Необходимые расширения
- **Python** - поддержка Python
- **Pylance** - языковой сервер
- **Black** - форматирование кода
- **Flake8** - линтинг
- **isort** - сортировка импортов

### Настройка VS Code
```json
{
    "python.defaultInterpreterPath": "./venv/bin/python",
    "python.linting.enabled": true,
    "python.linting.flake8Enabled": true,
    "python.formatting.provider": "black",
    "python.sortImports.args": ["--profile", "black"],
    "editor.formatOnSave": true,
    "editor.codeActionsOnSave": {
        "source.organizeImports": true
    }
}
```

### Настройка PyCharm
1. Откройте настройки (Ctrl+Alt+S)
2. Перейдите в "Tools" → "External Tools"
3. Добавьте Black и isort
4. Настройте автоматическое форматирование

---

## 🔍 Линтинг и форматирование

### Инструменты линтинга
- **Flake8** - проверка стиля кода
- **Black** - автоматическое форматирование
- **isort** - сортировка импортов
- **mypy** - проверка типов

### Конфигурация Flake8
```ini
[flake8]
max-line-length = 88
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    .pytest_cache
```

### Конфигурация Black
```toml
[tool.black]
line-length = 88
target-version = ['py311']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | venv
  | _build
  | buck-out
  | build
  | dist
)/
'''
```

### Конфигурация isort
```toml
[tool.isort]
profile = "black"
multi_line_output = 3
line_length = 88
known_first_party = ["meatbot"]
known_third_party = ["aiogram", "sqlalchemy", "redis"]
```

### Автоматическое исправление
```bash
# Форматирование кода
black .

# Сортировка импортов
isort .

# Проверка линтинга
flake8 .

# Проверка типов
mypy .
```

---

## 🔄 FSM (Finite State Machine)

### Обзор FSM
FSM (Finite State Machine) - это математическая модель вычислений, которая используется для управления состояниями в боте.

### Основные концепции
- **Состояние** - текущее состояние пользователя
- **Переход** - изменение состояния
- **Событие** - действие, вызывающее переход
- **Действие** - операция, выполняемая при переходе

### Реализация в aiogram
```python
from aiogram.fsm.state import State, StatesGroup

class ProductStates(StatesGroup):
    waiting_for_name = State()
    waiting_for_description = State()
    waiting_for_price = State()
    waiting_for_image = State()
```

### Использование состояний
```python
@router.message(ProductStates.waiting_for_name)
async def process_product_name(message: Message, state: FSMContext):
    await state.update_data(name=message.text)
    await message.answer("Введите описание товара:")
    await state.set_state(ProductStates.waiting_for_description)
```

### Управление состояниями
```python
# Установка состояния
await state.set_state(ProductStates.waiting_for_name)

# Получение данных состояния
data = await state.get_data()

# Обновление данных
await state.update_data(name="Новое название")

# Очистка состояния
await state.clear()
```

---

## 🏗️ Архитектура системы

### Общая архитектура
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Telegram Bot  │    │   Web Server    │    │   Database      │
│                 │    │                 │    │                 │
│  - Handlers     │    │  - API Routes   │    │  - PostgreSQL   │
│  - Middleware   │    │  - Health Check │    │  - Redis        │
│  - Keyboards    │    │  - Metrics      │    │  - Migrations   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 │
                    ┌─────────────────┐
                    │   Services      │
                    │                 │
                    │  - Cache        │
                    │  - Catalog      │
                    │  - Images       │
                    │  - Metrics      │
                    └─────────────────┘
```

### Структура проекта
```
meatbot/
├── app/
│   ├── handlers/          # Обработчики сообщений
│   ├── keyboards/         # Клавиатуры
│   ├── middlewares/       # Middleware
│   ├── services/          # Бизнес-логика
│   ├── database/          # Модели БД
│   ├── domains/           # Доменная логика
│   ├── interfaces/        # Интерфейсы
│   └── utils/             # Утилиты
├── alembic/               # Миграции БД
├── docker/                # Docker конфигурация
├── docs/                  # Документация
└── scripts/               # Скрипты
```

### Принципы архитектуры
- **Разделение ответственности** - каждый модуль отвечает за свою область
- **Инверсия зависимостей** - зависимости инжектируются извне
- **Единый интерфейс** - все сервисы реализуют интерфейсы
- **Тестируемость** - код легко тестируется

---

## 💡 Лучшие практики

### Разработка кода
1. **Следуйте PEP 8**
   - Используйте правильные отступы
   - Соблюдайте длину строк
   - Используйте понятные имена

2. **Документируйте код**
   - Добавляйте docstrings
   - Комментируйте сложную логику
   - Ведите changelog

3. **Тестируйте код**
   - Пишите unit тесты
   - Тестируйте интеграции
   - Используйте покрытие кода

### Работа с FSM
1. **Планируйте состояния**
   - Определите все возможные состояния
   - Создайте диаграмму переходов
   - Документируйте логику

2. **Управляйте данными**
   - Сохраняйте промежуточные данные
   - Валидируйте введенные данные
   - Очищайте состояние после завершения

3. **Обрабатывайте ошибки**
   - Предусматривайте неожиданные переходы
   - Добавляйте fallback состояния
   - Логируйте ошибки

### Настройка IDE
1. **Используйте виртуальные окружения**
   - Создавайте отдельные окружения для проектов
   - Используйте requirements.txt
   - Документируйте зависимости

2. **Настройте автоматическое форматирование**
   - Включите форматирование при сохранении
   - Настройте линтинг
   - Используйте pre-commit hooks

3. **Используйте отладку**
   - Настройте breakpoints
   - Используйте логирование
   - Тестируйте локально

---

## 🚀 Быстрый старт

### Настройка окружения
1. Создайте виртуальное окружение
2. Установите зависимости
3. Настройте IDE
4. Запустите линтинг

### Разработка
1. Создайте ветку для новой функции
2. Реализуйте функциональность
3. Напишите тесты
4. Отформатируйте код

### Тестирование
1. Запустите unit тесты
2. Проверьте линтинг
3. Тестируйте интеграции
4. Проверьте производительность

---

## 📊 Мониторинг и отладка

### Логирование
```python
import structlog

logger = structlog.get_logger()

# Логирование событий
logger.info("user_action", user_id=123, action="add_to_cart")

# Логирование ошибок
logger.error("database_error", error=str(e), query=query)
```

### Метрики
```python
from .services.metrics_service import get_metrics_service

metrics = get_metrics_service()
metrics.increment_counter("orders_created")
metrics.record_timing("database_query", duration)
```

### Health checks
```python
# Проверка состояния системы
GET /health/ready
GET /health/live
GET /metrics
```

---

## 📞 Поддержка

### Получение помощи
1. Изучите данную документацию
2. Проверьте логи системы
3. Обратитесь к разработчикам
4. Создайте issue в репозитории

### Контакты
- **Документация:** [docs/README.md](../README.md)
- **API:** [docs/api/API_REFERENCE.md](../api/API_REFERENCE.md)
- **Проблемы:** [docs/troubleshooting/ISSUES_AND_FIXES.md](../troubleshooting/ISSUES_AND_FIXES.md)

---

**Последнее обновление:** 20.10.2025
**Версия документации:** 1.0.0
